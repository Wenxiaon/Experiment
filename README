由于原始文件中的GPSR算法已经自带速度计算功能，为了更好的实验效果，把原始算法中自带的速度计算功能去除掉，再进行对比。同同时将GaussMarkovModel选作移动模型。
在上方的基础上加入了物理层信道的损耗，使用802.11p进行仿真。

次版本中除了以上改进，还修改了PosistionTable自动获取HelloInterval时隙，防止在之前的代码中虽然设置了时隙，但是PositionTable没有相应地更新，造成错误。
同时自动版本的routing-experiment文件将增加TCP仿真来仿新算法对时延的影响。

自上次提交版本后，发现物理层对数据包的解读存在问题，所以对功率感知模块进行了新的修改，改正此前存在的错误。